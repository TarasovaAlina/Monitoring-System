- Исходный код классов перенесен в src/
- Используются мои классы агентов
- IMetricCollector удален, получение метрик имплементировано внутри классов агентов
- IAgentLoader удален (создается лишь один производный класс и нет надобности в интерфейсе)
- IAgentService удален (аналогично с IAgentLoader)
- `nlohmann/` удалена, сторонние инструменты будут добавляться в виде статических библиотек в скрипте src/CMakeLists.txt
- `interfaces/` удалена за ненадобностью
- `include/agent_prototypes/` удалена, вместо нее используется `include/agent/`